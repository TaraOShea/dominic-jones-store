<div class="page">
  <div class="outer">
    <div class="container--s mxa">
      <h1 class="tu">{{ page.title }}</h1>

      <div class="rte">
        {{ page.content }}
      </div>
    </div>
  </div>
</div>